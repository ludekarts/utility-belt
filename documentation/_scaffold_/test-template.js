const { YYYYYYYY } = window.utilityBelt;


describe("YYYYYYYY", () => {

  it("Should exist an be a function", () => {
    chai.expect(YYYYYYYY).to.be.a("function");
  });

  it("Should XXXXXXX", () => {
    const source = AAAA;
    const result = YYYYYYYY(source);
    const expected = AAAA;
    chai.expect(result).to.eql(expected);
  });

});